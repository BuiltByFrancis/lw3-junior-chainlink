{
  "name": "lottery-game",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ builtbyfrancis/lottery-game",
    "create-local": "graph create --node http://localhost:8020/ builtbyfrancis/lottery-game",
    "remove-local": "graph remove --node http://localhost:8020/ builtbyfrancis/lottery-game",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 builtbyfrancis/lottery-game"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.31.0",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
